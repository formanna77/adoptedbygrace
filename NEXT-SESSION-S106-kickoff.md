# NEXT SESSION — S106 — PHASE D CONTINUES: THE NEXT REGISTER / MORE NET-NEW APEX BUILDS — ALL BORN CONSECRATED

**CONTEXT.** Campaign 2 — the tightening re-read — closed at S104 (589/589). Phase D reopened at S105 with the first new register since the campaign: the **Lutheran comparison family**. Three net-new apex pages shipped born CONSECRATED — `compare-lutheranism` (the flagship: two monergisms, the two forks are one fork), `compare-single-vs-double-predestination` (the crux theologorum + the Romans 9:22-23 grammatical asymmetry, §XV), `compare-can-the-saved-be-lost` (amissibility + the crown jewel pressed into perseverance). Prose 589 → 592; all SHARP on the cold-read; every verse/fact verified; comparisons-hub wired (8 → 11 cards). The site is no longer being repaired — it is being grown, at the campaign standard. S106 keeps growing.

**THE STANDARD FOR ALL NEW WORK (non-negotiable, unchanged).** The campaign bar is the FLOOR. Every new page, the SAME session it is written: (a) undeniable landing-force on whatever doorway a reader enters; (b) §XVI sapiential (impart wisdom, one earned sapiential line, end in awe not applause); (c) §XVII new-ground (break new ground — never re-wrap a known argument; three gates: the text bears it, consonant with the 2,000-year church, makes grace larger and the self smaller); (d) the two Campaign-2 axes (universal opener — no manufactured crisis scene; sharp prose — no double-landings/scaffolding/frame-bouncing); (e) cold-read the SAME session (born CONSECRATED — nothing ships UNREAD). Subagent cold-read on the macOS path → fix every non-SHARP on the main thread before it ships. Verify every verse/numeral/authority (web where needed) — the cold-read keeps catching fabrications every scanner misses (S105: it surfaced Luther's 1537 Capito letter that *confirmed* a claim; it would equally have caught a misquote).

**READ FIRST** (sections only — never read an `*-ARCHIVE.md` whole): `CLAUDE.md` (the manifesto — New-Ground + Sapiential + Sharp-prose/universal-openers) → `VOICE.md §XVI` (Sapiential) + `§XVII` (New-Ground) + `§I.2` + `§VI` (Manufactured Crisis Scene + Wordiness) + `§XV` (translation transparency) → `AUDIT-COVERAGE-LEDGER.md` (TALLY now **592**; the S105 born-CONSECRATED block) → `MISSION-CONTROL.md` Session 105 → `SAPIENTIAL-AUDIT.md §LI`. INTAKE: cross-device `mv` onto the repo mount FAILS — use `sed/awk … > /tmp/tmp && cat /tmp/tmp > FILE` for in-place rewrites and `cat tmp >> FILE` for appends; `rm` from workspace bash needs `allow_cowork_file_delete` first; helpers to `/tmp/s106_*` or the outputs dir. **CANONICAL-STRING TRAP (the fresh S105 lesson):** do NOT add `id="main-content"` — or anything — to the `<article class="article-body">` tag. The bare string is canonical: `sapiential-audit.js` keys prose-detection on the exact substring `<article class="article-body">`, so any addition silently drops the page from the prose count; the dangling `#main-content` skip-link is the 591-page site norm. Clone the head/nav/footer scaffold from an existing `compare-*` page and keep the article tag bare.

## PRIORITY 0 — STANDING PRE-FLIGHT (lean) + THE SAMPLE-READ AUDIT
1. `ls -1 *.html | wc -l` (expect 668 unless you build new pages). 2. `node sapiential-audit.js` (expect 592 prose / T1=0; rises as you add pages). 3. `node audit-prose.js` (v3 ✓ + v4 ✓, 0/668). 4. Hygiene greps, expect 0: truncated-meta `(name="description"|og:description|twitter:description) content="[^"]*…"`; double-encoded `&amp;(amp|apos|#0?39);`; `&amp;ldquo;`; banned emoji `[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}\x{FE0F}]`; `(NIV)` literal. 5. **THE STANDING P0 SAMPLE-READ AUDIT (CLAUDE.md):** live-read 5–10 existing pages (half locked + half unmarked) against the landing-force + sapiential test; any page that scans clean but reads slack → surgical-fix queue BEFORE new building. (S105's sample-read returned all-SHARP and cleared 2 real defects — an empty `<p>` on philosophy-problem-of-merit and a stray "In Brief." on systematic-reprobation — so the spot-check earns its keep.) 6. Never alter the canonical `<article class="article-body">` string.

## PRIORITY 1 — PHASE D CONTINUES: THE NEXT REGISTER / MORE APEX BUILDS
Pick ONE track and build 2–4 pages, all born CONSECRATED (the choice is yours/Aaron's):

**(A) EXTEND THE COMPARISON / SOTERIOLOGY REGISTER — recommended concrete first target: a dedicated page on "Does God Desire Everyone to Be Saved?"** The S105 Lutheran family named a third fork — universal grace, God's sincere will for all (1 Timothy 2:4, 2 Peter 3:9, Ezekiel 33:11) — but only touched it. It is the #1 shared proof-text of BOTH the Lutheran and the Arminian against unconditional election and definite atonement, and the site has NO dedicated page on it (grep confirmed at S105: no desires-all / all-men / 1-tim-2 / 2-pet-3 page exists). The new ground is the revealed/decretive (two-wills) distinction handled with §XVI wisdom and §XV honesty — *thelō* vs *boulomai*, the *pas* of 1 Tim 2:4, the "all" of 2 Pet 3:9 read in its covenant context — not the tired proof-text trade. ALTERNATIVE gaps in the same register: `compare-reformed-vs-eastern-orthodox` (theosis / synergism — a genuine gap) ; keep any sacramental/means-of-grace angle strictly soteriological (CLAUDE.md: "no eschatology; laser focus on God's sovereignty in salvation" — not a baptism-mode debate).

**(B) THE PHILOSOPHY-OF-THEOLOGY REGISTER** (the S105 alternative, still open) — divine simplicity and election, the grounding of modal truths in God, theological method — extending the S94 analytical seam (`analytical-*`). Pick this if you would rather open a genuinely new register than extend comparisons.

**(C) NET-NEW APEX BUILDS / Phase G Five-Point.** Flagship pages where the site is thin, or the Phase G Five-Point proof pages. Grep the existing set first (`ls *.html | grep <topic>`) so you EXTEND rather than duplicate (no third matt23-37-class twin).

**EVERY NEW PAGE — same-session checklist:** site structure (global.css link; verbatim `/_nav-template.html` nav; `nav.js` before `</body>`; the `footer-grace-warning` block; article pages wrap in exactly `<article class="article-body">` with `<header class="page-hero">` + `<span class="eyebrow">`); In-Brief box on top; 8–12 VERIFIED internal links woven Wikipedia-style (`ls name.html` before every href — never hallucinate; cross-category prioritized); deep-link relevant `/reformed-sources` PDFs (`%20` for spaces — e.g. `/Reformed%20Sources/TheBondageoftheWill-MartinLuther.pdf`); NIV 2011 only, no `(NIV)` tag, §XV transparency where the NIV under-translates; wire the new page into its hub the SAME session (add the `.hub-card` + bump the hub-stat count); then born-CONSECRATED cold-read (subagent macOS path → fix every non-SHARP) before it ships.

## PRIORITY 2 — STANDING CARRIES (unchanged)
(a) matt23-37 / matthew-23-37 duplicate-canonical — **Aaron's call** (both full overlapping pages; do not merge/delete unilaterally). (b) `/essays/` ESV ×2 — Aaron's call. (c) JSON-LD STRUCTURED-DATA pass (LOW priority, not user-visible, out of prose scope) — generic-boilerplate `description` fields + ~20 pages whose JSON-LD publisher `logo` points at a nonexistent `/logo.png` (og-image.png is the site norm). (d) The two diversification queues (Box A/B; Three-Responses-That-Fail) are EXHAUSTED — vary one only if a future full read finds a NEW generic instance; the canonical homes stay.

## SPECIAL — STANDING PROTOCOL
(a) v3 + v4 clean before AND after; `</article>` POSITION not count (related-articles lives INSIDE article-body — the close `</article>` comes AFTER RELATED-ARTICLES-START); never alter the canonical `<article class="article-body">` string (the S97 + S105 lesson). (b) Manual §II banned-move grep on every new/touched page (no 2am/3am/midnight/odd-hour/clock reader-tableaux; subject-native night register for grief/insomnia/historical/parable is fine). (c) NIV 2011 only; §XV; verify every verse/numeral/authority on a new page (web where needed) BEFORE it ships — the cold read has repeatedly caught fabricated quotes/stats that every scanner misses. (d) Close pipeline ×9: `node build-search-index.js`, `build-mega-menu.js`, `build-homepage-counts.js`, `auto-linker.js`, `wire-orphans.js`, then `validate-site.js`, `canonical-conformance.js`, `verify-scripture.js` (≈268 standing divergent is noise; (NIV) files must be 0), then `sapiential-audit.js` + `audit-prose.js`. Post-pipeline re-grep `&amp;ldquo;` + truncated-meta + double-encoded (expect 0). Do NOT run `fix-meta-descriptions.js` or `build-related-articles.js` (the latter re-injects `cat.emoji` at line 204 → undoes the S74 emoji sweep; card-desc fixes are ALWAYS a manual desc-only sweep). (e) Pre-close context audit — fill the window; do not close early; floor of real new apex pages per run, not one-and-done. (f) Ledger: log each new page born-CONSECRATED (TALLY now 592). MISSION-CONTROL Session 106 (newest-at-top). SAPIENTIAL-AUDIT.md §LII. (g) HOUSEKEEPING: at close, roll the oldest live log entry to the `*-ARCHIVE.md` (append at BOTTOM) IF the live log exceeds 6 (after adding S106/§LII: live should be S101–S106 / §XLVII–§LII → roll Session 100 + §XLVI). (h) NO git — Aaron pushes.

## CLOSE
Pipeline GREEN ×9; append `## §LII. S106 EXECUTION LOG`; ledger entries for the new pages + updated TALLY; MISSION-CONTROL Session 106 (newest-at-top); the archive roll if the live log exceeds 6; print the push command; write `NEXT-SESSION-S107-kickoff.md` (continue Phase D — the next register / next apex builds, all born CONSECRATED).

**THE REAL FINISH-LINE (restated).** The site is being grown, not repaired. Phase D makes the site larger without ever lowering the bar: every new page must carry the undeniable force, impart wisdom, break new ground, open universally, say it in as few words as the truth needs, and end the reader in awe — and be read the same day it is written. Build the next register. Break new ground. Keep the steel and the tenderness. Nothing ships UNREAD.
