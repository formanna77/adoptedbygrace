# NEXT SESSION — S108 — PHASE D CONTINUES: THE NEXT REGISTER / MORE NET-NEW APEX BUILDS — ALL BORN CONSECRATED

**CONTEXT.** Phase D keeps growing the site at the campaign standard: S105 the Lutheran family (3), S106 the EO comparison + free-offer (2), and S107 deepened the doctrine-of-God / analytical seam with 3 born CONSECRATED — `analytical-divine-simplicity` (a whim requires parts; "Until Him, you had only ever been loved by committees"; Calvin's mirror as the catch), `analytical-impassibility` (the burning bush as the doctrine in a picture; conditional election prosecuted as covert passibilism, the cause/criterion escape welded; "You cannot lose a love you never caused"), `analytical-immutability` (the Mal 3:6 *SO*; time-as-solvent — "Everything you have ever lost, you lost in time. You are held by a choice that was never in it"). Prose 594 → 597; all three TRIM→SHARP on the same-session cold-read (22 fixes); every verse web-verified BEFORE drafting; philosophy-hub cards 33–35, hub-stat 32→35. **S107 also repaired two standing wiring gaps: the entire analytical register had been invisible to the mega-menu since S94 and uncounted by the homepage philosophy tile — `analytical` prefix added to BOTH `build-mega-menu.js` (UMBRELLAS prefixes) and `build-homepage-counts.js` (CATEGORIES patterns); mega-menu 633→641, homepage-counts 552→560.** The site is being grown, not repaired. S108 keeps growing.

**THE LESSON BANK (both live).** (1) S106's: grep titles AND H1s AND content before building (`grep -il "<concept>" *.html` + title/H1 extraction), read the closest 2–3 neighbors full, EXTEND never duplicate. (2) S107's new one: **when you open a register with a NEW filename prefix, add the prefix to `build-mega-menu.js` AND `build-homepage-counts.js` the same session** — hub-wiring alone leaves the register invisible to the menu and the homepage tiles.

**THE STANDARD FOR ALL NEW WORK (non-negotiable, unchanged).** Every new page, the SAME session it is written: (a) undeniable landing-force on whatever doorway a reader enters; (b) §XVI sapiential (impart wisdom, one earned sapiential line, end in awe not applause); (c) §XVII new-ground (three gates: the text bears it; consonant with the 2,000-year church; grace larger, self smaller); (d) the two Campaign-2 axes (universal opener — no manufactured crisis scene; sharp prose — no double-landings/scaffolding/frame-bouncing); (e) cold-read the SAME session (born CONSECRATED — nothing ships UNREAD): subagent cold-read on the macOS path → fix every non-SHARP main-thread before ship. Verify every verse/numeral/authority (web BEFORE drafting — S107's fetch-first flow caught "moths and vermin" vs the assumed "moth and rust" and the parallagē/parallax cognate nuance; it works, keep it).

**READ FIRST** (sections only — never read an `*-ARCHIVE.md` whole): `CLAUDE.md` (the manifesto) → `VOICE.md §XVI` + `§XVII` + `§I.2` + `§VI` + `§XV` → `AUDIT-COVERAGE-LEDGER.md` (TALLY now **597**; the S107 born-CONSECRATED block) → `MISSION-CONTROL.md` Session 107 → `SAPIENTIAL-AUDIT.md §LIII`. INTAKE: cross-device `mv` onto the repo mount FAILS — use `sed/awk … > /tmp/tmp && cat /tmp/tmp > FILE` for in-place rewrites and `cat tmp >> FILE` for appends; `rm` from workspace bash needs `allow_cowork_file_delete` first; helpers to `/tmp/s108_*` or the outputs dir. **CANONICAL-STRING TRAP (still live):** do NOT add `id="main-content"` — or anything — to the `<article class="article-body">` tag; clone the scaffold from an `analytical-*` or `compare-*` page and keep the article tag bare.

## PRIORITY 0 — STANDING PRE-FLIGHT (lean) + THE SAMPLE-READ AUDIT
1. `ls -1 *.html | wc -l` (expect **673** unless you build new pages). 2. `node sapiential-audit.js` (expect **597 prose / T1=0**; rises as you add pages). 3. `node audit-prose.js` (v3 ✓ + v4 ✓, 0/673). 4. Hygiene greps, expect 0: truncated-meta `(name="description"|og:description|twitter:description) content="[^"]*…"`; double-encoded `&amp;(amp|apos|#0?39);`; `&amp;ldquo;`; banned emoji `[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}\x{FE0F}]`; `(NIV)` literal. 5. **THE STANDING P0 SAMPLE-READ AUDIT (CLAUDE.md):** live-read 5–10 existing pages (mix locked + unmarked, across registers) against the landing-force + sapiential + two-axes test; any page that scans clean but reads slack → surgical-fix queue BEFORE new building. (S107's sample returned 2 SHARP / 4 TRIM — the TRIMs included a HAMMER-LOCKED page whose garbled sentence accidentally stated faith-precedes-life. The sample-read keeps paying; do not skip it.) 6. Never alter the canonical `<article class="article-body">` string.

## PRIORITY 1 — PHASE D CONTINUES: THE NEXT REGISTER / MORE APEX BUILDS
Pick ONE track and build 2–4 pages, all born CONSECRATED. **Grep-first before each build (the S106 lesson; S107 re-proved the discipline works in both directions).**

**(A) FINISH THE DOCTRINE-OF-GOD SEAM — recommended first target: `analytical-archetypal-ectypal` (theological method — how creatures know God truly without knowing Him exhaustively).** Confirmed thin at S107: no page on theological method anywhere on the site. The seam: Deut 29:29 + 1 Cor 13:12 + the Creator/creature distinction in *knowing* — God's self-knowledge (archetypal) vs the creaturely copy He accommodates to us (ectypal, Calvin's "lisping nurse"); why the doctrines of grace humble the rationalist AND the mystic; why "I can't fully understand election" is not an objection but the expected shape of all true knowledge of God. New ground: every modern reader assumes Google-epistemology — all knowledge flat, total, on-demand — and resents a God who keeps a roster sealed; the page names this century's epistemic idol. CAUTION: S107's immutability page now owns the time-solvent material — a dedicated `analytical-eternality` page would risk a twin; grep hard and prefer method/knowledge over time. ALTERNATIVES in-register (grep first): omnipotence and the cross; the grounding of modal truths in God beyond the Molinism page.

**(B) MORE COMPARISON / SOTERIOLOGY BUILDS** — candidates to CHECK (not assume): a dedicated Wesleyan/Methodist distinctives page (prevenient grace owns `demolition-prevenient-grace`; a full `compare-wesleyan-methodism` may still be a genuine gap); a Provisionism ("Traditionalist" SBC / Leighton Flowers) full comparison beyond `response-leighton-flowers` + `response-sbc-traditional-statement`. Keep strictly soteriological.

**(C) NET-NEW APEX BUILDS / Phase G Five-Point.** Flagship pages where the site is thin, or the Phase G Five-Point proof pages. Grep the existing set first so you EXTEND rather than duplicate.

**EVERY NEW PAGE — same-session checklist:** site structure (global.css; verbatim nav; nav.js before `</body>`; footer-grace-warning; `<article class="article-body">` bare + `<header class="page-hero">` + `<span class="eyebrow">`); In-Brief on top; 8–12 VERIFIED woven links (`ls name.html` before every href; cross-category prioritized; auto-linker raises the count); deep-link a relevant `/reformed-sources` PDF (`%20` for spaces); NIV 2011 only, no `(NIV)`, §XV transparency where the NIV under-translates; wire into its hub the SAME session (hub-card + hub-stat bump) **and if the page opens a NEW filename prefix, add it to build-mega-menu.js + build-homepage-counts.js (the S107 lesson)**; verify every verse by web fetch BEFORE drafting; born-CONSECRATED cold-read (subagent macOS path → fix every non-SHARP) before ship.

## PRIORITY 2 — STANDING CARRIES (unchanged)
(a) matt23-37 / matthew-23-37 duplicate-canonical — **Aaron's call** (do not merge/delete unilaterally). (b) `/essays/` ESV ×2 — Aaron's call. (c) JSON-LD STRUCTURED-DATA pass (LOW, not user-visible): generic-boilerplate `description` fields + ~20 pages with JSON-LD publisher `logo` → nonexistent `/logo.png`. (d) Diversification queues EXHAUSTED — vary only if a future read finds a NEW generic instance.

## SPECIAL — STANDING PROTOCOL
(a) v3 + v4 clean before AND after; `</article>` POSITION not count; never alter the canonical `<article class="article-body">` string. (b) Manual §II banned-move grep on every new/touched page (no 2am/3am/midnight/odd-hour/crisis-triage reader-tableaux; subject-native night register for grief/insomnia/historical/parable is fine). (c) NIV 2011 only; §XV; verify every verse/numeral/authority BEFORE it ships. (d) Close pipeline ×9: `node build-search-index.js`, `build-mega-menu.js`, `build-homepage-counts.js`, `auto-linker.js`, `wire-orphans.js`, then `validate-site.js`, `canonical-conformance.js`, `verify-scripture.js` (**~270 standing divergent is noise** [268 + 2 legitimate S107 fragment-variants]; (NIV) files must be 0), then `sapiential-audit.js` + `audit-prose.js`. **New standing baselines: search-index 670 / mega-menu 641 / homepage-counts 560 / wire-orphans ~70 unmapped (67 + the 3 new analytical pages — manually hub-wired, the register has never been in wire-orphans' map) — all rise if you add pages.** Post-pipeline re-grep `&amp;ldquo;` + truncated-meta + double-encoded (expect 0). Do NOT run `fix-meta-descriptions.js` or `build-related-articles.js` (the latter re-injects `cat.emoji` → undoes the S74 emoji sweep). (e) Pre-close context audit — fill the window; do not close early. (f) Ledger: log each new page born-CONSECRATED (TALLY now 597). MISSION-CONTROL Session 108 (newest-at-top). SAPIENTIAL-AUDIT.md §LIV. (g) HOUSEKEEPING: at close, roll the oldest live entry to the `*-ARCHIVE.md` (append at BOTTOM) IF the live log exceeds 6 (after adding S108/§LIV: live would be S102–S108 / §XLVIII–§LIV → roll Session 102 + §XLVIII). (h) NO git — Aaron pushes.

## CLOSE
Pipeline GREEN ×9; append `## §LIV. S108 EXECUTION LOG`; ledger entries for the new pages + updated TALLY; MISSION-CONTROL Session 108 (newest-at-top); the archive roll; print the push command; write `NEXT-SESSION-S109-kickoff.md` (continue Phase D — the next register / next apex builds, all born CONSECRATED).

**THE REAL FINISH-LINE (restated).** The site is being grown, not repaired. Every new page must carry the undeniable force, impart wisdom, break new ground, open universally, say it in as few words as the truth needs, and end the reader in awe — and be read the same day it is written. Grep first so you build new ground, not a twin. Wire every new prefix all the way through (hub + menu + homepage). Keep the steel and the tenderness. Nothing ships UNREAD.
