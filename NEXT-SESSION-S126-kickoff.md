# NEXT SESSION — S126 — FACT-VERIFICATION BLITZ, LEG 2 (remaining named authors → then Scripture sweep)

**BUILDS ARE PAUSED.** Aaron launched the Fact-Verification Blitz on 2026-06-16 after the 70%-defect-rate finding (21/30 sampled pages across S119–S124 carried a factual/scriptural/attribution defect). The blitz is a class-based site-wide sweep of the highest-risk categories, in order: **(1) named-person quotations, (2) Scripture wording, (3) stats/dates.** No new pages until the blitz closes. The held Phase-D build plan waits in `NEXT-SESSION-PHASE-D-RESUME-kickoff.md`.

**CONTEXT — what Leg 1 (S125) did (do NOT redo):** Spurgeon — the most-abused name (152 pages) — swept. 9 defect-classes fixed across 11 pages (fabricated free-will + earthquake + particle-of-dust-tail quotes de-quoted; the apocryphal "yellow stripe" quip de-quoted/unified on 3 pages; "kiss the wave" hedged; a loose election paraphrase + a wrong sermon cite corrected; "sun in the sky"→"heavens"; an unlocatable Calvin "means/ends" line de-quoted). A large clean block was PROTECTED (the "Calvinism is the gospel… we preach… nowadays" wording is correct — a subagent's "you preach" fix was itself wrong; systematic-reprobation's "too wise to err" is genuine; Polycarp's "Eighty-six years" correctly attributed). Full log: MISSION-CONTROL.md Session 125 + SAPIENTIAL-AUDIT.md §LXXI + AUDIT-COVERAGE-LEDGER.md S125.

**TWO FLAGGED ITEMS — fix these FIRST (verify before touching):**
1. `analogy-chess-grandmaster` (~line 265) — an Augustine quote: "Thus the will is truly free when it is not the servant of vices… God's foreknowledge does not compel events; rather, it foresees that which free agents will freely choose." The second half reads as a modern foreseen-choice gloss — **theologically backwards for Augustine** (the architect of predestination-by-grace), so likely fabricated/conflated. Web-verify against Augustine's actual corpus; correct or de-quote.
2. `question-does-god-love-everyone-equally` (~line 342) — "God does not love us because we are valuable. We are valuable because God loves us," attributed to Augustine but **commonly attributed to C. S. Lewis / others**. Verify true source; re-attribute or de-quote.

## PRIORITY 0 — PRE-FLIGHT (lean)
`ls -1 *.html | wc -l` (**682**); `node sapiential-audit.js` (T1 0; 606 prose); `node audit-prose.js` (v3+v4 0/682); hygiene greps (emoji / "(NIV)" / double-encoded / truncated-meta) all 0. (Use `;` / `|| true` between `grep -c` calls.)

## PRIORITY 1 — LEG 2A: REMAINING NAMED-AUTHOR QUOTATIONS
Sweep the next authors after Spurgeon, highest-volume / highest-risk first: **Augustine, Calvin, Luther, Edwards, Owen, Bunyan, Whitefield, Lewis, Chesterton, Pink, Tozer, Boston, Newton, Watson, Flavel, Sproul, Packer, Machen, Warfield.** Method (proven in Leg 1):
1. Rebuild the per-author extractor (ephemeral `/tmp`). It walks every `<p>/<blockquote>/<li>/<h>` block, and for blocks that name the author (or whose preceding block does) it pulls `<blockquote>`s and any in-block quoted span ≥25 chars; dedupes by file+quote; ignores JSON-LD/meta/titles/Scripture. Run `node extract_author.js <name>` per author.
2. Dispatch parallel READ-ONLY subagents (macOS-path Read tool), one per author or author-cluster, to web-verify each quote against the author's actual corpus. **Treat every in-quotes attribution as GUILTY until verified.** The common failures: popular paraphrase printed as verbatim; one author's line attributed to another (esp. famous names); a real quote welded to a fabricated tail; a quote hung on a wrong source/sermon.
3. Apply fixes: de-quote fabrications to attributed paraphrase, restore verbatim wording for miswordings, re-attribute misattributions, drop wrong source-cites. **verify-don't-blind-strip** (it saved 4 pages in Leg 1 — confirm the page is wrong before "fixing" it). Banned-move grep + bare `<article class="article-body">` check on every touched page; NIV-2011-exact for any Scripture touched.

## PRIORITY 2 — LEG 2B (if budget remains): SCRIPTURE-WORDING SWEEP
Triage the **271 verify-scripture "divergent" flags** (`node verify-scripture.js`; the report is `scripture-audit-report.txt`) — we have been calling these "standing noise," but the blitz exists to find the ESV/KJV/NASB phrases masquerading as NIV hiding among them. Work the report in batches; web-check each flagged verse against NIV 2011; fix the genuine bleed-throughs; leave the true false-positives. This directly defends the "open your own Bible" promise.

## PRIORITY 3 — (later legs) STATS / DATES
Historical claims, statistics, dates across the corpus (the S120 Dort delegate-count and S124 Machen telegram-count classes). Defer until quotations + Scripture are clean.

## CLOSE
Pipeline GREEN ×9 (build-search-index, build-mega-menu, build-homepage-counts, auto-linker, wire-orphans, then validate-site, canonical-conformance, verify-scripture [(NIV) must be 0], sapiential-audit, audit-prose). NO build-sitemap/IndexNow (the blitz adds no pages). `node build-og-cards.js --check` (re-render only if a pull-quote on an og-card page changed). Ledger entry; MISSION-CONTROL entry (newest-at-top); SAPIENTIAL-AUDIT §LXXII (append at BOTTOM); roll the oldest live entry if logs exceed 6 (live now: S120–S125 / §LXVI–§LXXI — adding S126/§LXXII will require rolling S120 + §LXVI via the `_roll.js` helper — update its REPO/OUT mount paths + the §/Session markers first). Pre-close context audit; print the push command; write `NEXT-SESSION-S127-kickoff.md` (Blitz Leg 3, or — if the quotation + Scripture sweeps are complete — the resume-builds brief pointing back to `NEXT-SESSION-PHASE-D-RESUME-kickoff.md`).

**Current counts at S125 close: 606 prose / 682 HTML / 681 sitemap / 650 mega-menu / 569 homepage / 59 og-cards / search-index 679.**

**AARON-BOUND QUEUE (account-bound; nag in the close box):** push (S124 + S125-blitz fixes — 13 pages total); the first true week-over-week Netlify read at `/logs-and-metrics/analytics`; the GSC dashboard; the `docs/outreach/SEND-CHECKLIST.md` pitch sends + the Reformation21 follow-up (now ~32+ days); Substack 01 → 05; Bing import; the audio decision.

**THE MEASURE (restated for the blitz).** The site's entire apologetic is one promise: *go check it yourself, and what we told you will hold.* A 70% defect rate among sampled pages means that promise is, right now, breakable — not because the theology is wrong but because the corpus was written by a confabulator and blessed by a prose-reader. The blitz makes the promise true again, one verifiable class at a time. Leg 1 took the worst name (Spurgeon) off the table. Leg 2 takes the rest of the names, then the verses. Distrust the polish. Open the source. Find the real words before you let a single quotation stand.
