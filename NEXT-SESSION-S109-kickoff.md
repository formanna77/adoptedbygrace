# NEXT SESSION — S109 — CAMPAIGN 3, ROADS LEG 2: READ THE INSTRUMENTS, WIDEN THE ROADS

**CONTEXT.** S108 opened Campaign 3 and shipped Roads leg 1: the machine-trust surface is repaired (real /logo.png; **8 pages of malformed JSON-LD fixed — Google can now parse 100% of the structured data**; request-indexing queue written; IndexNow wired, fires after Aaron's next deploy), the **visible staircase is live** (benchmarking gap #3 CLOSED — start-here rebuilt with the five-phase journey strip + Phase-N-of-5 cards + per-phase hand-offs), and all Lane 2 artifacts are send-ready (Monergism pitch; 3 resource-page pitches vs live-verified targets; Substack drafts 01+02; outreach-log). The P0 sample-read caught and fixed 38 trims across 5 pages incl. a misrepresented Calvin citation and two non-NIV quotes on POLISH-LOCKED demolition-2peter3-9 — locks are not invulnerabilities; the sample-read keeps paying. Cadence: **2 Roads : 1 build** until indexed ≥ ~300 or 90 days — **S110 is the build session: paste `NEXT-SESSION-PHASE-D-RESUME-kickoff.md`.**

**READ FIRST** (sections only — never read an `*-ARCHIVE.md` whole): `CLAUDE.md` → `MISSION-CONTROL.md` Session 108 → `SAPIENTIAL-AUDIT.md` §LIV (the CARRIES list) → `docs/outreach-log.md`. INTAKE: cross-device `mv` onto the repo mount FAILS — use `sed/awk … > /tmp/tmp && cat /tmp/tmp > FILE`; helpers to `/tmp/s109_*`. **CANONICAL-STRING TRAP (live):** never alter the bare `<article class="article-body">` string on any page you touch.

## PRIORITY 0 — STANDING PRE-FLIGHT + SAMPLE-READ
1. `ls -1 *.html | wc -l` (expect **673**). 2. `node sapiential-audit.js` (597 prose / T1=0). 3. `node audit-prose.js` (v3 ✓ + v4 ✓). 4. Hygiene greps (truncated-meta / double-encoded / `&amp;ldquo;` / emoji / `(NIV)`) — expect 0. 5. **THE STANDING P0 SAMPLE-READ:** live-read 5 pages (mix locked + unmarked, none from the S108 five) on landing-force + opener-universality + wordiness; surgical-fix anything slack BEFORE Roads work — S108's read returned 0/5 SHARP, so expect real work here.

## PRIORITY 1 — LANE 1: READ THE INSTRUMENTS (Aaron's numbers in, adjust)
(a) **Log Aaron's GSC re-read** in `docs/outreach-log.md` KPI table (indexed count, coverage, flagship crawl status). If indexed is climbing: stay the course. If "Crawled — currently not indexed" balloons: authority is the bottleneck — shift session weight to Lane 2 prep. (b) Confirm the IndexNow ping fired (Aaron runs `node indexnow-ping.js` post-deploy; expect 200/202) and Bing verification/import happened; log it. (c) Verify live deploy serves /logo.png and the repaired JSON-LD (fetch 2–3 of the 8 fixed pages on the live domain and re-parse). (d) Netlify Analytics: if Aaron toggled it, note baseline traffic in the log.

## PRIORITY 2 — LANE 3: THE CHEAP MULTIPLIER + (IF VOICE CHOSEN) THE AUDIO PILOT
(a) **Per-page OG quote-cards for the flagship 50** (deep-dive Lane 3 item 3): the SVG quote-card pipeline has 3 pilots — batch-extend it so the request-indexing-queue's 30 + the next 20 flagships stop all wearing the same og-image.png when shared. Page-specific, truthful, palette-true; wire each page's og:image + twitter:image; verify with a parse pass. (b) **If Aaron has chosen the audio voice** (his voice vs TTS): execute the pilot on the top 10 flagships per his choice — player markup is trivial, keep it class-based, no inline styles. If no decision yet, skip without nagging (it stays on his queue).

## PRIORITY 3 — LANE 2: KEEP THE PITCHES FLOWING (prep; Aaron sends)
(a) Log any responses to the S108 sends in `docs/outreach-log.md`; draft replies if any came back warm. (b) **Personalize Pitch #2 (guest essay)** for ONE target — Aquila Report OR Reformation21 OR Place for Truth (pick by fit after reading their current guest guidelines; verify the submission channel live) — offering "Where Did Your Faith Come From?" written fresh for their audience. (c) Find + verify 2 more resource-page targets (the plan's search patterns; reject anything compromised or stale — GraceNet UK is already on the rejected list). (d) Substack drafts 03–04 if the cadence needs runway (next strongest: a story-tender or pastoral piece + an essay; question-* pages stay excluded per protocol).

## SPECIAL — STANDING PROTOCOL (unchanged)
NO new pages. If you touch HTML: v3+v4 clean before AND after; banned-move grep on touched pages; NIV 2011 + §XV; close pipeline ×9 (`build-search-index`, `build-mega-menu`, `build-homepage-counts`, `auto-linker`, `wire-orphans`, `validate-site`, `canonical-conformance`, `verify-scripture` [~270 standing divergent is noise; (NIV) must be 0], `sapiential-audit` + `audit-prose`); do NOT run `fix-meta-descriptions.js` or `build-related-articles.js`. Pre-close context audit. **NO git — Aaron pushes.**

## CLOSE
Pipeline GREEN ×9 (if HTML touched); MISSION-CONTROL Session 109 entry (newest-at-top; roll S103 to archive — live log holds 6); SAPIENTIAL-AUDIT.md §LV (roll §XLIX); update `docs/outreach-log.md`; print the push command; write `NEXT-SESSION-S110-kickoff.md` as a one-line pointer: **S110 is the build session — paste `NEXT-SESSION-PHASE-D-RESUME-kickoff.md` and execute it under Phase D standards (born-CONSECRATED, cold-read same session)**, with Roads leg 3 following at S111.

**THE FINISH-LINE.** The roads are under construction: the crawler can finally read every page, the staircase is visible, and four pitches sit in Aaron's outbox. This leg reads the first instrument data and widens what's working. Indexed pages is still the number. Keep the steel and the tenderness — and get them found.
