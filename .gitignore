# Local Netlify folder
.netlify
deploy-*.zip
node_modules/

# Session scratch backups (mount-locked; never deploy)
.s137-bak/
*-bak/

# macOS junk
.DS_Store

# FUSE mount artifacts (orphaned when a file is deleted while open)
.fuse_hidden*
