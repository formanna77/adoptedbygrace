# Rolled MISSION-CONTROL entries


### Session 202 — 2026-08-10 — **BOTH OF THE OPEN P0 DEFECTS WERE ALREADY FIXED AND THE INSTRUMENTS WERE LYING ABOUT IT; AND THE PHONE-WIDTH LAW HAS BEEN PRESCRIBING A TEST THAT CANNOT SEE THE BUG IT WAS WRITTEN ABOUT**

*Three doorway pages lifted. The session's real yield is that **three consecutive sessions of "still broken" were measurement error**, and the standing render law was blind by construction.*

**§A — P0a WAS NEVER BROKEN AFTER S201. THE FETCHER WAS.** Two sessions declared the four `410!` rules dead on the strength of a tool reporting `Content-Type: application/javascript`. A plain browser `GET` — no cache-buster, no `cache:'reload'` — returns **410 `text/html`, the 19,189-byte 404 page**, for all four, and for `/build-tags.js`, `/stamp-modified.js`, `/detect-feeling-slip.js`, `/CLAUDE.md`, `/VOICE.md` and `archive/`. `mcp__workspace__web_fetch` appears to infer content-type from the URL extension rather than read the response, and renders a non-2xx body as `[binary data]`. **S201's law was right and incomplete: only an HTTP request can confirm a redirect — and only one that speaks HTTP the way a reader's browser does.** Canonized as CLAUDE.md law 12.

**§B — P0b WAS ALSO ALREADY FIXED, AND A STALE CACHE NEARLY REOPENED IT.** First measurement on `/analogy-the-prodigal-already-home` showed the authorial sentence still computing to `rgb(184,169,232)` — `--accent-scripture` — with the purple gradient and the quote glyph, and `blockquote.sop-pullquote` **absent from the CSSOM entirely**. But `fetch('/global.css')` on that same page returned the file *with* the rule, and `replaceSync` on those exact bytes parsed all three rules. The `<link>` had been served from the HTTP cache: the loaded sheet was **exactly three rules shorter** than the bytes on the server. Re-attached with a cache-buster, it is **gold** (`rgb(212,162,84)`), `::after` `none`, no gradient. Same law 12.

**§C — LAW 8 PRESCRIBED A TEST THAT CANNOT SEE `white-space: nowrap`.** The law said to walk elements comparing `getBoundingClientRect().right` against the viewport. **That is structurally blind to the exact defect the law cites as its reason for existing.** `body { overflow-x: hidden }` makes body a scroll container, so a block child's *border box is clamped* while its ink spills past. Proven: a 100-character `nowrap` `<p>` injected into a 390px page measured `right: 375` — dead inside the viewport — with 1,127px of content. Under `rect.right` alone **nineteen pages scored a clean zero**, and the first probe was simply broken. The working test is **`scrollWidth - clientWidth`** run alongside `rect.right`. Also established: skip any element whose *own* `overflow-x` is not `visible` (the `footer` glow and `.chain-hero` report 113px and 279px and are **false positives** — both clip themselves deliberately); and `X-Frame-Options: DENY` blocks `<iframe src>`, so use a **`srcdoc` iframe with an injected `<base>`** — no HTTP response, same-origin, true 390px viewport. That is the method S201 could not find.

**§D — WHAT THE WORKING PROBE FOUND.** **`.related-articles-grid` is defined twice**, and the mobile override — `@media (max-width: 768px) { grid-template-columns: 1fr }` at ~line 2786 — is **dead**, shadowed by an identical-specificity rule 245 lines later. At 390px the track resolved to a hard `280px` inside a 231px container: **the right 49px of every related-article card clipped, on 631 pages.** Fixed with `minmax(min(280px, 100%), 1fr)`, and the same wrap applied to **66 fixed-px `minmax` floors** across `global.css` + 8 inline styles (no-op above the floor, self-healing below, breakpoint-independent). Second: the breadcrumb `<li>`s carried `min-width: 0` under `flex-wrap: nowrap`, so a long page title squeezed **"Home" into a 21px box holding 37px of ink — the word rendering straight through the `›` separator** on every article page. `flex-shrink: 0` on all crumbs, `1` on the last. Verified: psychology-why-we-resist 7 overflows → 1; "Home" `clientWidth` 22 → 37; zero overlaps.

**§E — THE THREE COMPRESSION CUTS S201 ORDERED, TAKEN.** **Owen 4,209 → 3,742.** Cut the second consecutive Greek excursus (`pan ho`/`ex autou`), the clause answering an objection no reader raised, halved Acts 13:48 keeping the steelman-and-answer, and reduced three images carrying one point to one. **Augustine 3,846 → 3,620**, and the strongest section rebuilt on solid ground: the page claimed Augustine argued from the **Greek** *helkyō* against "gentle wooing." He worked from the Latin *traxerit*, says in the *Confessions* how little Greek ever stayed, and in Tractate 26 glosses the drawing **by delight** — Virgil's *trahit sua quemque voluptas*, web-verified. The true argument is far better: *irresistible and nobody dragged, both at once — He does not overpower what you want, He replaces it.* Also: Orange 529 condemned **semi**-Pelagianism, and its canons were **lost until the sixteenth century**, so "settled catholic teaching for a thousand years" was wrong — and the truth is the better story, because it explains why the Reformers look like innovators and were not.

**§F — BRADWARDINE: THE LARGEST MISS IN THE COHORT, CLOSED.** The page is titled *The Mathematician Who Proved God's Sovereignty*, asserts "equations" six ways, and **never showed one line of mathematics.** Cut the beat stated six times before the biography begins; fixed the H2 `Forty Days` → `Thirty-Eight Days` (last survivor of the 40/38 contradiction S201 harmonized); retired the **chalk/chalkboard anachronism** (x3 — a 19th-century instrument in 1349) for a wax tablet. Then added the real thing, web-verified: *De proportionibus velocitatum* (1328), **Bradwardine's rule** — arithmetic gain in effect requires *geometric* gain in the ratio of force to resistance, a logarithm written nearly three centuries before the word. And the theological seam (§XVII), stated honestly as ours and not put in his mouth: **at a ratio of one the velocity is not small, it is zero, and beneath that threshold every addition of force buys exactly nothing.** Which is the whole quarrel with the New Pelagians — they described a cart that moves slowly; Scripture describes one that does not move. *Grace does not improve the ratio. Grace changes what is being weighed.* 3,083 → 3,419.

**Carries forward.** **P0a and P0b are CLOSED — do not re-open them; re-verify in a browser, not a fetcher.** **Boston and Anselm were NOT reached** and carry S201's findings unaddressed: Boston's four-deep closes and the 620-word linkless section; Anselm's *Cur Deus Homo* overreach, the false claim that Aquinas "could not finally evade Anselm's God" (he **rejected** the ontological argument), and *possit* → **potest**. Inline `style=` **713/233** unchanged. Gospel-absence queue 133. 21 scripts on the brittle article literal. 159 untagged locks. `canonical-conformance.js` prints `Pages OK: 686` against `Pages checked: 685` — an off-by-one in its own tally, harmless, worth a look.

**Pipeline** GREEN. **687 HTML / 611 canonical prose** · search-index 683 · manifest 606 · mega-menu 655 · homepage 611 / tiles changed 0 · all-content 611 · auto-linker 1 · wire-orphans 0 orphans / 75 unmapped · **ALL 20 CHECKS PASSED** · CHECK 10 713/233 · CHECK 17 **62, none added** · CHECK 18 2,058 blocks parse · CHECK 19 **5,369 (was 5,381) — ratchet lowered in `validate-site.js`** · sitemap 685 URLs, XML parses · `dateModified` == `<lastmod>` verified on all three pages. CHECK 11 failed once mid-run (auto-linker had just added a link) and was cleared by `dedupe-prose-links.js`, 3 duplicates removed. 0 emoji, one `article-body` per page, every new href verified on disk.

**── END-OF-SESSION STANDING ──**
Since S201 (2026-08-10): at §XXIII 108 -> 108 (+0);  remaining 510 -> 510 (+0).
Remaining trend: S195:568  S196:559  S197:525  S198:525  S199:520  S200:520  S201:510  S202:510
*(+0 is correct and expected: all three pages lifted this session were already at §XXIII from S201. This run bought depth on pages already counted, plus three corpus-wide render fixes the counter does not measure.)*

---
