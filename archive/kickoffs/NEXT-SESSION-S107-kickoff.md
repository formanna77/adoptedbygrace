# NEXT SESSION — S107 — PHASE D CONTINUES: THE NEXT REGISTER / MORE NET-NEW APEX BUILDS — ALL BORN CONSECRATED

**CONTEXT.** Campaign 2 (the tightening re-read) closed at S104 (589/589). Phase D has been growing the site ever since at the campaign standard: S105 shipped the Lutheran comparison family (3 pages, 589→592); S106 shipped two more born CONSECRATED — `compare-reformed-vs-eastern-orthodox` (the East has the highest destination in Christendom, *theosis*, and the lowest door, synergy on a will it admits is only *wounded* — the whole quarrel reduces to *how dead is the patient?*) and `question-free-offer-of-the-gospel` (hyper-Calvinism and Arminianism are twins — both assume the offer's sincerity requires *ability* in the sinner; a command measures what you owe, not what you can — Edwards's natural/moral inability). Prose 592 → 594; both SHARP on the cold-read; every verse/authority verified; comparisons-hub 11→12, questions-hub 77→78. The site is being grown, not repaired. S107 keeps growing.

**THE HARD LESSON FROM S106 — GREP THE EXISTING SET *THOROUGHLY* BEFORE BUILDING.** The S106 kickoff's recommended flagship ("Does God Desire Everyone to Be Saved?" / the two-wills page) was **already saturated** — `question-godwants` (titled "Doesn't God Want Everyone Saved? — The Two Wills of God"), `objection-desire`, `demolition-1tim2-4`, `demolition-2peter3-9`, `demolition-ezekiel33-11` all own it. The S105 grep that reported "no page exists" had used the wrong search terms. Building it would have been a matt23-37-class twin. **Before building ANY page: grep titles AND H1s AND content for the topic, not just filenames** (`grep -il "<concept>" *.html` + `grep -oE '<title>...' / '<h1>...'`), and read the closest 2–3 neighbors in full to confirm you are EXTENDING, not duplicating. The pivot saved the session; bake the discipline in. (Saturated, do NOT rebuild: the desires-all / two-wills / 1 Tim 2:4 / 2 Pet 3:9 / Ezek 33:11 cluster; the free-offer / hyper-Calvinism / duty-faith ground; the Lutheran + Eastern-Orthodox comparisons.)

**THE STANDARD FOR ALL NEW WORK (non-negotiable, unchanged).** The campaign bar is the FLOOR. Every new page, the SAME session it is written: (a) undeniable landing-force on whatever doorway a reader enters; (b) §XVI sapiential (impart wisdom, one earned sapiential line, end in awe not applause); (c) §XVII new-ground (break new ground — never re-wrap a known argument; three gates: the text bears it, consonant with the 2,000-year church, makes grace larger and the self smaller); (d) the two Campaign-2 axes (universal opener — no manufactured crisis scene; sharp prose — no double-landings/scaffolding/frame-bouncing); (e) cold-read the SAME session (born CONSECRATED — nothing ships UNREAD). Subagent cold-read on the macOS path → fix every non-SHARP on the main thread before it ships. Verify every verse/numeral/authority on a new page (web where needed) BEFORE it ships — the cold-read keeps catching what every scanner misses.

**READ FIRST** (sections only — never read an `*-ARCHIVE.md` whole): `CLAUDE.md` (the manifesto) → `VOICE.md §XVI` (Sapiential) + `§XVII` (New-Ground) + `§I.2` (opener) + `§VI` (Manufactured Crisis Scene + Wordiness) + `§XV` (translation transparency) → `AUDIT-COVERAGE-LEDGER.md` (TALLY now **594**; the S106 born-CONSECRATED block) → `MISSION-CONTROL.md` Session 106 → `SAPIENTIAL-AUDIT.md §LII`. INTAKE: cross-device `mv` onto the repo mount FAILS — use `sed/awk … > /tmp/tmp && cat /tmp/tmp > FILE` for in-place rewrites and `cat tmp >> FILE` for appends; `rm` from workspace bash needs `allow_cowork_file_delete` first; helpers to `/tmp/s107_*` or the outputs dir. **CANONICAL-STRING TRAP (still live):** do NOT add `id="main-content"` — or anything — to the `<article class="article-body">` tag. `sapiential-audit.js` keys prose-detection on the exact bare substring `<article class="article-body">`; any addition silently drops the page from the prose count (the dangling `#main-content` skip-link is the site norm). Clone the head/nav/footer scaffold from an existing `compare-*` or `question-*` page and keep the article tag bare.

## PRIORITY 0 — STANDING PRE-FLIGHT (lean) + THE SAMPLE-READ AUDIT
1. `ls -1 *.html | wc -l` (expect 670 unless you build new pages). 2. `node sapiential-audit.js` (expect 594 prose / T1=0; rises as you add pages). 3. `node audit-prose.js` (v3 ✓ + v4 ✓, 0/670). 4. Hygiene greps, expect 0: truncated-meta `(name="description"|og:description|twitter:description) content="[^"]*…"`; double-encoded `&amp;(amp|apos|#0?39);`; `&amp;ldquo;`; banned emoji `[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}\x{FE0F}]`; `(NIV)` literal. 5. **THE STANDING P0 SAMPLE-READ AUDIT (CLAUDE.md):** live-read 5–10 existing pages (half locked + half unmarked) against the landing-force + sapiential test; any page that scans clean but reads slack → surgical-fix queue BEFORE new building. (S106's sample-read returned all-SHARP across demolition-1tim2-4, demolition-2peter3-9, objection-desire, demolition-ezekiel33-11.) 6. Never alter the canonical `<article class="article-body">` string.

## PRIORITY 1 — PHASE D CONTINUES: THE NEXT REGISTER / MORE APEX BUILDS
Pick ONE track and build 2–4 pages, all born CONSECRATED (the choice is yours/Aaron's). **Grep-first before each build (the S106 lesson).**

**(A) OPEN THE PHILOSOPHY-OF-THEOLOGY REGISTER — recommended concrete first target: `analytical-divine-simplicity` (divine simplicity and election).** The S94 analytical seam (`analytical-aseity-and-decree`, `analytical-grounding-objection-molinism`, `analytical-identity-regeneration`, `analytical-information-and-imago`, `analytical-modal-collapse-answered`) is the site's youngest register and is genuinely thin — confirmed at S106 that there is NO page on divine simplicity, impassibility, immutability, or theological method. The new ground: if God is *simple* (His attributes are identical with His essence and with each other), then His decree to elect is not an arbitrary add-on to a God who could have done otherwise on a whim — election is bound up with who He is; mercy and will and being are one act. Handle with §XVI wisdom and §XV honesty; the seam where analytic theology meets the doctrines of grace is real new ground the fathers could not work. ALTERNATIVES in the same register: the grounding of modal truths in God; aseity and the freedom of the decree; theological method / archetypal-ectypal knowledge.

**(B) MORE COMPARISON / SOTERIOLOGY BUILDS** — the `compare-*` shelf is now broad (Arminian, Catholic, Molinist, Open-Theist, Lutheran, Eastern-Orthodox, monergism-synergism, single/double, predestination/foreknowledge, can-the-saved-be-lost, every-decision-point, grace-reformed-vs-arminian). Before adding, grep hard for a survivor gap — candidates to CHECK (not assume): a dedicated Wesleyan/Methodist distinctives page, or a Provisionism ("Traditionalist" SBC / Leighton Flowers) full treatment beyond the existing `response-leighton-flowers` + `response-sbc-traditional-statement`. Keep strictly soteriological (no eschatology, no sacramental-mode debates).

**(C) NET-NEW APEX BUILDS / Phase G Five-Point.** Flagship pages where the site is thin, or the Phase G Five-Point proof pages. Grep the existing set first (`ls *.html | grep <topic>`) so you EXTEND rather than duplicate.

**EVERY NEW PAGE — same-session checklist:** site structure (global.css link; verbatim `/_nav-template.html` nav; `nav.js` before `</body>`; the `footer-grace-warning` block; article pages wrap in exactly `<article class="article-body">` with `<header class="page-hero">` + `<span class="eyebrow">`); In-Brief box on top; 8–12 VERIFIED internal links woven Wikipedia-style (`ls name.html` before every href — never hallucinate; cross-category prioritized; auto-linker will raise the count); deep-link a relevant `/reformed-sources` PDF (`%20` for spaces — e.g. `/Reformed%20Sources/TheFreedomoftheWill-JonathanEdwards.pdf`, `/Reformed%20Sources/Institutes%20-%20John%20Calvin.pdf`); NIV 2011 only, no `(NIV)` tag, §XV transparency where the NIV under-translates; wire the new page into its hub the SAME session (add the `.hub-card` + bump the hub-stat count); then born-CONSECRATED cold-read (subagent macOS path → fix every non-SHARP) before it ships.

## PRIORITY 2 — STANDING CARRIES (unchanged)
(a) matt23-37 / matthew-23-37 duplicate-canonical — **Aaron's call** (both full overlapping pages; do not merge/delete unilaterally). (b) `/essays/` ESV ×2 — Aaron's call. (c) JSON-LD STRUCTURED-DATA pass (LOW priority, not user-visible, out of prose scope) — generic-boilerplate `description` fields + ~20 pages whose JSON-LD publisher `logo` points at a nonexistent `/logo.png` (og-image.png is the site norm). (d) The two diversification queues (Box A/B; Three-Responses-That-Fail) are EXHAUSTED — vary one only if a future full read finds a NEW generic instance.

## SPECIAL — STANDING PROTOCOL
(a) v3 + v4 clean before AND after; `</article>` POSITION not count (related-articles lives INSIDE article-body — the close `</article>` comes AFTER RELATED-ARTICLES-START); never alter the canonical `<article class="article-body">` string. (b) Manual §II banned-move grep on every new/touched page (no 2am/3am/midnight/odd-hour/clock reader-tableaux; subject-native night register for grief/insomnia/historical/parable is fine). (c) NIV 2011 only; §XV; verify every verse/numeral/authority on a new page (web where needed) BEFORE it ships. (d) Close pipeline ×9: `node build-search-index.js`, `build-mega-menu.js`, `build-homepage-counts.js`, `auto-linker.js`, `wire-orphans.js`, then `validate-site.js`, `canonical-conformance.js`, `verify-scripture.js` (≈268 standing divergent is noise; (NIV) files must be 0), then `sapiential-audit.js` + `audit-prose.js`. Post-pipeline re-grep `&amp;ldquo;` + truncated-meta + double-encoded (expect 0). Do NOT run `fix-meta-descriptions.js` or `build-related-articles.js` (the latter re-injects `cat.emoji` at line 204 → undoes the S74 emoji sweep; card-desc fixes are ALWAYS a manual desc-only sweep). (e) Pre-close context audit — fill the window; do not close early; floor of real new apex pages per run, not one-and-done. (f) Ledger: log each new page born-CONSECRATED (TALLY now 594). MISSION-CONTROL Session 107 (newest-at-top). SAPIENTIAL-AUDIT.md §LIII. (g) HOUSEKEEPING: at close, roll the oldest live log entry to the `*-ARCHIVE.md` (append at BOTTOM) IF the live log exceeds 6 (after adding S107/§LIII: live would be S102–S107 / §XLVIII–§LIII → roll Session 101 + §XLVII). (h) NO git — Aaron pushes.

## CLOSE
Pipeline GREEN ×9; append `## §LIII. S107 EXECUTION LOG`; ledger entries for the new pages + updated TALLY; MISSION-CONTROL Session 107 (newest-at-top); the archive roll if the live log exceeds 6; print the push command; write `NEXT-SESSION-S108-kickoff.md` (continue Phase D — the next register / next apex builds, all born CONSECRATED).

**THE REAL FINISH-LINE (restated).** The site is being grown, not repaired. Phase D makes the site larger without ever lowering the bar: every new page must carry the undeniable force, impart wisdom, break new ground, open universally, say it in as few words as the truth needs, and end the reader in awe — and be read the same day it is written. Grep first so you build new ground, not a twin. Break the next register. Keep the steel and the tenderness. Nothing ships UNREAD.
