# NEXT SESSION — S105 — PHASE D REOPENS: EXPANSION UNBLOCKED AT 589/589 — THE SECOND REGISTER + NET-NEW APEX BUILDS

**CONTEXT.** Campaign 2 — the tightening re-read — **CLOSED at S104 (2026-06-09): 589/589.** Every prose doorway on the site has now been re-read on the two axes Campaign 1 never carried — (1) opener universality (no manufactured crisis scene, no Circular Return to a scene that didn't land, no failed central analogy) and (2) wordiness/sharpness (no double-landings, bare-echo closes, scaffolding, frame-bouncing, somatic-clone saturation). Six straight down-ladder legs (S98→S104) ran clean: the manufactured-scene class is exhausted below the top of the ladder; the wordiness axis carried the tail. **Aaron's expansion hold lifts at 589/589.** S105 reopens Phase D. The site stands sharp, universal, and fully re-read — and now it grows again, at the campaign standard.

**THE STANDARD FOR ALL NEW WORK (non-negotiable).** The campaign bar is the FLOOR, not the ceiling, for every page born from here on. Every new page must, the SAME session it is written: (a) carry the undeniable landing-force on whatever doorway a reader enters (CLAUDE.md "THE STANDARD"); (b) pass §XVI sapiential (impart wisdom, not merely argue correctly — one earned sapiential line, end in awe not applause); (c) pass §XVII new-ground (break new ground — the undiscovered angle / never-forged analogy / newly-named idol / cross-disciplinary synthesis — never re-wrap a known argument; three gates: text bears it, consonant with 2,000-year church, makes grace larger and the self smaller); (d) pass the two Campaign-2 axes (universal opener, sharp prose); (e) be cold-read the same session (born CONSECRATED — nothing ships UNREAD). Use a subagent cold-read on the macOS path, then fix every non-SHARP on the main thread before it ships.

**READ FIRST** (sections only — never read an `*-ARCHIVE.md` whole): `CLAUDE.md` (the manifesto — esp. the New-Ground Standard + the Sapiential Standard + Sharp-prose/universal-openers) → `VOICE.md §XVI` (Sapiential Register) + `§XVII` (New-Ground Mandate) + `§I.2 (amended)` + `§VI` (Manufactured Crisis Scene + Wordiness — the floor for new openers) + `§XV` (translation transparency) + `Move 10` → `AUDIT-COVERAGE-LEDGER.md` (TALLY + the CAMPAIGN 2 CLOSED block) → `MISSION-CONTROL.md` Session 104 → `SAPIENTIAL-AUDIT.md §L`. INTAKE: cross-device `mv` onto the repo mount FAILS — use `sed/awk … > /tmp/tmp && cat /tmp/tmp > FILE` for in-place rewrites and `cat tmp >> FILE` for appends; `rm` from workspace bash needs `allow_cowork_file_delete` first; helpers to `/tmp/s105_*` or the outputs dir.

## PRIORITY 0 — STANDING PRE-FLIGHT (lean) + THE SAMPLE-READ AUDIT
1. `ls -1 *.html | wc -l` (expect 665 unless you build new pages). 2. `node sapiential-audit.js` (expect 589 prose / T1=0; rises as you add pages). 3. `node audit-prose.js` (v3 ✓ + v4 ✓, 0/665). 4. Regression greps ROOT-scoped, expect canon-only (the standing set — see SAPIENTIAL-AUDIT §L / the S104 ledger leg-log for the live-only signatures; nothing should have cloned). 5. Hygiene greps, expect 0: truncated-meta `(name="description"|og:description|twitter:description) content="[^"]*…"`; double-encoded `&amp;(amp|apos|#0?39);`; `&amp;ldquo;`; banned emoji `[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}\x{FE0F}]`; `(NIV)` literal. 6. **THE STANDARD's standing P0 Sample-Read Audit (CLAUDE.md):** live-read 5–10 existing pages (half locked + half unmarked) against the landing-force + sapiential test; any page that scans clean but reads slack goes onto a surgical-fix queue BEFORE new building. (Campaign 2 closed the two-axis re-read; this is the ongoing landing-force/sapiential spot-check that never stops.) 7. Never alter the canonical `<article class="article-body">` string (the S97 light-switch lesson).

## PRIORITY 1 — PHASE D: THE SECOND REGISTER + NET-NEW APEX BUILDS
Aaron's directive at the campaign close: reopen Phase D — open a register the site is thin on, and add net-new apex pages. Two tracks (do the build track; the choice of register is yours/Aaron's):

**(A) THE SECOND REGISTER — recommended concrete first target: `compare-lutheranism`** (Reformed vs. Lutheran soteriology — election, perseverance/falling-away, the bound will, sacramental grace, the universal-vs-particular call). The site already has a strong `compare-*` family (calvinism-arminianism, -molinism, -open-theism, -catholic-soteriology, monergism-synergism, predestination-foreknowledge, grace-reformed-vs-arminian, every-decision-point) but NO Lutheran comparison — a genuine gap, and a charitable steel-man-then-distinguish page is high-value. ALTERNATIVE register: **philosophy-of-theology** (analytical-theology's neighbor — e.g., divine simplicity and election, the grounding of modal truths in God, theological method) extending the S94 Phase-D analytical seam. Pick ONE register and build 2–4 pages in it this session.

**(B) NET-NEW APEX BUILDS / Phase G Five-Point.** Beyond the register, build net-new flagship pages where the site is thin, or the Phase G Five-Point proof pages. Each must break NEW ground (§XVII), not re-wrap an existing page — grep the existing set first (`ls *.html | grep <topic>`) so you extend rather than duplicate (do NOT create a third matt23-37-class duplicate).

**EVERY NEW PAGE — same-session checklist:** site structure (global.css link; verbatim `/_nav-template.html` nav; `nav.js` before `</body>`; the `footer-grace-warning` block; article pages wrap in exactly `<article class="article-body">` with `<header class="page-hero">` + `<span class="eyebrow">`); In-Brief box on top (the 90-second on-ramp); 8–12 VERIFIED internal links woven Wikipedia-style (cross-category prioritized; `ls name.html` before every href — never hallucinate a target); deep-link relevant `/reformed-sources` PDFs (`%20` for spaces); NIV 2011 only, no `(NIV)` tag, §XV transparency where the NIV under-translates; wire the new page into its hub the SAME session (add the `.hub-card`); then born-CONSECRATED cold-read (subagent on the macOS path → fix every non-SHARP) before it ships.

## PRIORITY 2 — STANDING CARRIES (unchanged)
(a) matt23-37 / matthew-23-37 duplicate-canonical — **Aaron's call** (both full overlapping pages; do not merge/delete unilaterally; S104 sharpened only matthew-23-37's opener). (b) `/essays/` ESV ×2 — Aaron's call. (c) JSON-LD STRUCTURED-DATA pass (LOW priority, not user-visible, out of prose scope) — 448 generic-boilerplate `description` fields + 20 pages whose JSON-LD publisher `logo` points at a nonexistent `/logo.png` (og-image.png is the 659-page norm). (d) The two diversification queues (Box A/B; Three-Responses-That-Fail) are EXHAUSTED — vary one only if a future full read finds a NEW generic instance; the canonical homes stay (philosophy-problem-of-merit's Answer A/B; the academic "three responses" register on 2-thessalonians-2-13 + apologetic-the-double-grip-of-john-10).

## SPECIAL — STANDING PROTOCOL
(a) v3 + v4 clean before AND after; `</article>` POSITION not count (related-articles lives INSIDE article-body — the close `</article>` comes AFTER RELATED-ARTICLES-START); never alter the canonical `<article class="article-body">` string. (b) Manual §II banned-move grep on every new/touched page (no 2am/3am/midnight/odd-hour/clock/"at this very hour"/"scrolling on your screen" reader-tableaux; subject-native night register for grief/insomnia/historical/parable is fine). (c) NIV 2011 only; §XV; verify every verse/numeral/authority on a new page (web where needed) BEFORE it ships — the cold read has repeatedly caught fabricated quotes/stats that every scanner misses. (d) Close pipeline ×9: `node build-search-index.js`, `build-mega-menu.js`, `build-homepage-counts.js`, `auto-linker.js`, `wire-orphans.js`, then `validate-site.js`, `canonical-conformance.js`, `verify-scripture.js` (≈268 standing divergent is noise; (NIV) files must be 0), then `sapiential-audit.js` + `audit-prose.js`. Post-pipeline re-grep `&amp;ldquo;` + truncated-meta + double-encoded (expect 0). Do NOT run `fix-meta-descriptions.js` or `build-related-articles.js` (the latter re-injects `cat.emoji` at line 204 → undoes the S74 emoji sweep; card-desc fixes are ALWAYS a manual desc-only sweep). (e) Pre-close context audit — fill the window; do not close early; floor of real new apex pages per run, not one-and-done. (f) Ledger: log each new page born-CONSECRATED (the post-campaign convention — the campaign tally is closed; new work logs as CONSECRATED-at-birth with its cold-read verdict). MISSION-CONTROL Session 105 (newest-at-top). SAPIENTIAL-AUDIT.md §LI. (g) HOUSEKEEPING: at close, roll the oldest live log entry to the `*-ARCHIVE.md` (append at BOTTOM) IF the live log exceeds 6 (after adding S105/§LI: live should be S100–S105 / §XLVI–§LI → roll Session 99 + §XLV). (h) NO git — Aaron pushes.

## CLOSE
Pipeline GREEN ×9; append `## §LI. S105 EXECUTION LOG`; ledger entries for the new pages + updated TALLY; MISSION-CONTROL Session 105 (newest-at-top); the archive roll if the live log exceeds 6; print the push command; write `NEXT-SESSION-S106-kickoff.md` (continue Phase D — the next register pages / next apex builds, all born CONSECRATED).

**THE REAL FINISH-LINE (restated).** The site is no longer being repaired; it is being grown. Campaign 2 made every existing doorway sharp and universal. Phase D makes the site larger without ever lowering the bar: every new page must carry the undeniable force, impart wisdom, break new ground, open universally, say it in as few words as the truth needs, and end the reader in awe — and be read the same day it is written. Build the second register. Break new ground. Keep the steel and the tenderness. Nothing ships UNREAD.
